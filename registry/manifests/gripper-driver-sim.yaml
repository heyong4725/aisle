id: gripper-driver-sim
kind: node
provides: [gripper_actuation]
requires: []
inputs:
  gripper_cmd: {schema: scalar_f32, rate_hz: 30}
outputs:
  gripper_state: {schema: scalar_f32, latency_class: hard_rt}
embodiment:
  arm: [franka, so101]
  gripper: parallel
safety_class: motion
# eval null pending M0 evalcards from TC-A1..A3 runs (CAP-6 exception, ADR-3)
eval: null
origin: hub
source: src/aisle/nodes/dora_genesis.py
