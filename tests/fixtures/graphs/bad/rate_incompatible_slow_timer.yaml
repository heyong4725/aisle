# Warning-only fixture on a non-simulator node: task-state-machine declares
# its feedback tick at 1 Hz, while this 0.1 Hz timer is below the TC-4 band.
nodes:
- id: task-state-machine
  inputs:
    tick: dora/timer/millis/10000
  outputs:
  - episode_feedback
