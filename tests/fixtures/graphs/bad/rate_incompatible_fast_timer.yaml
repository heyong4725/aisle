# warning-only fixture on a LAUNCHABLE node (see slow_timer note): genesis
# tick declares 100 Hz, a 500 Hz timer is far above the TC-4 band
nodes:
- id: dora-genesis
  inputs:
    tick: dora/timer/millis/2
  outputs:
  - poses
