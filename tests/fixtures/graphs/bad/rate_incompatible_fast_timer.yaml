# Warning-only fixture on a non-simulator node: task-state-machine declares
# its feedback tick at 1 Hz, while this 500 Hz timer is above the TC-4 band.
nodes:
- id: task-state-machine
  inputs:
    tick: dora/timer/millis/2
  outputs:
  - episode_feedback
