# minimal L0 perception wiring on LAUNCHABLE nodes (rewritten for VAL-2
# INSTALL_MISSING: the previous detector-openvocab version was schema-valid
# but could never launch — exactly the H1-discovered class)
nodes:
- id: dora-genesis
  inputs:
    tick: dora/timer/millis/10
  outputs:
  - poses
- id: oracle-pose
  inputs:
    poses: dora-genesis/poses
  outputs:
  - target_pose
