# Minimal L0 perception wiring on installed non-simulator registry nodes.
# A sim_bridge is never accepted without ADR-30 clock topology.
nodes:
- id: camera-source
  outputs:
  - poses
- id: oracle-pose
  inputs:
    poses: camera-source/poses
  outputs:
  - target_pose
