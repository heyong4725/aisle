{
  "allocation_ratio": 1.0,
  "alpha": 0.05,
  "analysis_seed": 500001,
  "artifact_claim_threshold": 0.9,
  "bootstrap": {
    "confidence_level": 0.95,
    "replicates": 10000
  },
  "campaign_id": "cse-causal-study-pilot-v1",
  "campaign_phase": "pilot",
  "control_arm": "monolithic",
  "decision": {
    "kind": "superiority",
    "margin": null,
    "rationale": "equivalence and non-inferiority are not the primary decision for this template"
  },
  "exclusion_rules": [
    "exclude only a predeclared infrastructure failure independent of treatment outcome",
    "retain every exclusion row and reason in the raw records",
    "no reruns: a failed attempt is retained under a new session id and never overwritten"
  ],
  "experimental_unit": "session",
  "inclusion_rules": [
    "retain and classify every randomized assignment",
    "include a started session unless a predeclared infrastructure rule applies"
  ],
  "outcome": {
    "direction": "higher",
    "field": "session_success",
    "type": "binary"
  },
  "power": {
    "assumption_note": "control_probability 0.40 is the pre-registration ASSUMPTION carried from cse-causal-study-v1; this pilot protocol exists to REPLACE it with pilot-only control success (CSE-8). Pilot rows are calibration inputs only and never enter a confirmatory estimate (STA-3). 6 sessions per arm is below the CSE-8 floor of 10 by design (ADR-66).",
    "control_probability": 0.4,
    "sensitivity_effects": [
      0.15,
      0.2,
      0.25,
      0.3
    ],
    "test_sidedness": "one-sided",
    "treatment_probability": 0.65
  },
  "primary_estimand": {
    "contrast": "typed_dataflow minus monolithic",
    "population": "autonomous coding-agent sessions on the frozen task band",
    "summary": "risk_difference"
  },
  "protocol_id": "cse-causal-study-pilot-v1-protocol-r0",
  "schema_version": "aisle.stats.protocol.v1",
  "smallest_effect": {
    "measure": "risk_difference",
    "value": 0.25
  },
  "stopping_rule": {
    "max_sessions_per_arm": 6,
    "type": "fixed"
  },
  "strata": [
    "agent_system",
    "task"
  ],
  "survival": {
    "censor_time_field": "wall_s",
    "event_time_field": "accepted_time_s"
  },
  "target_power": 0.8,
  "treatment_arm": "typed_dataflow",
  "treatment_arms": [
    "monolithic",
    "typed_dataflow"
  ],
  "zero_event": {
    "confidence_level": 0.95,
    "event_field": "safety_events",
    "exposure_field": "commands",
    "exposure_unit": "command"
  }
}
