# Treatment-difference table mon-pilot-l2-treatment-v1 (generated; SPEC 440 MON-1)

Regenerate with `uv run harness monolith table --write`; CI checks it.

| id | surface | class | typed arm | monolithic arm | justification | analysis treatment |
|---|---|---|---|---|---|---|
| mon1-01 | agent deliverable | intentionally-different | capability manifests + dora dataflow YAML + node modules of the agent region<br>`graphs/pilot_t1_l2_typed.yaml` bba4b7002dcc<br>`registry/manifests/detected-pose.yaml` 46d1899fa6ee<br>`registry/manifests/grasp-planner-topdown.yaml` fb4d4696b908<br>`registry/manifests/ik-trajectory.yaml` e2b498f5a47d<br>`registry/manifests/task-state-machine.yaml` 5af70b46a412<br>`src/aisle/nodes/l2_pose.py` f5576aa27eed<br>`src/aisle/nodes/grasp_topdown.py` 3f24e0a56571<br>`src/aisle/nodes/ik_trajectory.py` 44e8186838d9<br>`src/aisle/nodes/task_state_machine.py` ad7fcc819c7b | one orchestration module against the primitive API<br>`experts/monolithic/pilot_t1_l2.py` 2ed42b4e76fb<br>`docs/monolithic/pilot-l2/allowlist.json` 1f426fbde402 | this is the treatment: bundled typed-dataflow engineering surface versus one ordinary module | the manipulated factor (SPEC 500 primary contrast) |
| mon1-02 | static validation and diagnostics | intentionally-different | harness validate (VAL catalog: schema, topology, motion gating, turn plan)<br>`src/aisle/harness/validate.py` ed82967e800a | none; Python syntax/import/runtime errors only, via harness monolith check<br>`src/aisle/harness/monolith.py` a2cbf377cbd7 | part of the bundled treatment; absence is a declared property, not missing data (MON-12) | recorded as treatment property; check/run attempt and error events compared descriptively |
| mon1-03 | registry search and resolution | intentionally-different | registry manifests, harness registry search/lint<br>`src/aisle/harness/registry.py` 214da7bb8db2 | absent; the primitive API is the only capability catalog<br>`docs/monolithic/pilot-l2/primitive-api.md` bd1e27aadf18 | part of the bundled treatment | treatment property |
| mon1-04 | documentation given to the agent | intentionally-different | research-agent contract and the typed graph/manifest docs<br>`harness/CLAUDE.research.md` e736bbd6caa7 | primitive API document and module contract<br>`docs/monolithic/pilot-l2/primitive-api.md` bd1e27aadf18<br>`docs/monolithic/pilot-l2/README.md` c55be9588b26 | each arm documents its own representation; task, goals, budgets and evaluator text are shared | documentation hashes in the MON-8 treatment tuple; representation-specific instructions listed here rather than in prose |
| mon1-05 | robot primitives (pose, grasp, trajectory, executor) | identical | Pinned L2 RGB/depth estimator, grasp planner, trajectory solver and streamer used by typed workers<br>`src/aisle/nodes/l2_pose.py` f5576aa27eed<br>`src/aisle/nodes/grasp_topdown.py` 3f24e0a56571<br>`src/aisle/nodes/ik_trajectory.py` 44e8186838d9 | the same function objects re-exported by aisle.monolith.primitives<br>`src/aisle/monolith/primitives.py` 450b0ead8726 | none needed: same artifact | Implementation identity held fixed; L2 primitive parity checked by tests/unit/test_monolith_l2_primitives.py |
| mon1-06 | semantic observations | representation-equivalent | Public L2 calibration, RGB/depth, joint state, T1 goal, reset notification and guard feedback<br>`graphs/pilot_t1_l2_typed.yaml` bba4b7002dcc | The same public sensor and task fields as broker events; no oracle verdicts<br>`graphs/pilot_t1_l2_monolithic.yaml` 19ccc2f835e1<br>`docs/monolithic/pilot-l2/interface-map.json` 34e235ba0f2b | none needed: same semantic fields, different envelope | Semantic field sets checked against both graphs; full runtime leakage qualification remains pending |
| mon1-07 | actions and authority | representation-equivalent | joint_cmd / gripper_cmd edges into budget-guard, episode_feedback to rollout-client<br>`graphs/pilot_t1_l2_typed.yaml` bba4b7002dcc | action dicts {joint_cmd}, {gripper_cmd}, {feedback} validated and published by the broker on the same edges<br>`src/aisle/nodes/monolith_broker.py` 6fb86d2fdcae | none needed: same semantic fields, different envelope | held fixed; same names, arity and privilege (interface map) |
| mon1-08 | guard route and limits | identical | budget-guard node, harness/limits + budget.toml<br>`src/aisle/nodes/budget_guard.py` e15bc5f70b29<br>`harness/budget.toml` bcbb79aa146a | the same node consuming monolith-broker/joint_cmd and gripper_cmd<br>`graphs/pilot_t1_l2_monolithic.yaml` 19ccc2f835e1 | none needed: same artifact | held fixed (MON-5) |
| mon1-09 | trusted controller (bridge, reset, verifier, rollout client, turn barrier) | identical | Bridge, reset, frozen oracle, fixed-horizon rollout client, public projection and turn barrier<br>`src/aisle/nodes/dora_genesis.py` 196837073251<br>`src/aisle/reset/service.py` 289a8c80374d<br>`src/aisle/verifier/oracle.py` c4ce22de0b88<br>`src/aisle/harness/rollout_client.py` 803707bc9b08<br>`src/aisle/nodes/turn_barrier.py` fd075658ecc8<br>`src/aisle/harness/pilot_policy_surface.py` bb08dc54cf6a | Same trusted controllers and public projection; one policy broker<br>`src/aisle/nodes/monolith_broker.py` 6fb86d2fdcae | none needed: same artifact | Controller identity held fixed; full timing and leakage qualification still required |
| mon1-10 | transport metadata | intentionally-different | nodes see dora metadata (sim_time_ns, seq, env_id, turn stamps, h/w/enc)<br>`src/aisle/topics.py` 431a0e0bee4c | the module sees sim_time_ns only; frames arrive reshaped<br>`src/aisle/nodes/monolith_broker.py` 6fb86d2fdcae | runtime envelope needed only by the typed transport; carries no task authority | excluded from task authority (MON-4); no analysis treatment |
| mon1-11 | turn accounting | intentionally-different | four lockstep participants in the agent region<br>`graphs/turn_plans/pilot_t1_l2_typed.json` 5605916f9f05 | one participant (the broker)<br>`graphs/turn_plans/pilot_t1_l2_monolithic.json` 64d6b3bd4159 | runtime representation of the same synchronous step; ordering of the four typed nodes is fixed by the plan, so the merged participant observes the same data per turn | no task-outcome treatment; wall-time is reported per arm, not compared causally |
| mon1-12 | in-episode retry budget | intentionally-different | AISLE_MAX_RETRIES=8 attested in the graph<br>`graphs/pilot_t1_l2_typed.yaml` bba4b7002dcc | module-internal decision, bounded by the same episode timeout<br>`experts/monolithic/pilot_t1_l2.py` 2ed42b4e76fb | the typed arm attests retries in YAML because that is where its decisions live; the episode budget (timeout, seeds) is held by the rollout runner in both arms | retries reported per episode in both arms (episode_feedback.retries) |
| mon1-13 | episode budget, seeds, reset semantics, evaluator | identical | harness rollout --graph graphs/pilot_t1_l2_typed.yaml<br>`src/aisle/harness/rollout.py` 09de8b848a5f | harness monolith run --module (calls the same rollout())<br>`src/aisle/harness/monolith.py` a2cbf377cbd7 | none needed: same artifact | Same fixed simulation deadline; oracle results remain private until deadline; no policy verdict input |
| mon1-14 | confinement | intentionally-different | Isolated authored node workers under the admitted external adapter and private controller<br>`specs/460-actuation-threat-model.md` 27c3701afb0b | Isolated module worker through a trusted primitive broker and admitted external adapter<br>`src/aisle/monolith/confinement.py` a8b561ea3dc4 | Different worker representations require separately bound policies; identical excluded privileges | New exact controller/surface needs conformance qualification; independent isolation review remains pending |
