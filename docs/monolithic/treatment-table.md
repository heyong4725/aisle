# Treatment-difference table mon-treatment-table-v1 (generated; SPEC 440 MON-1)

Regenerate with `uv run harness monolith table --write`; CI checks it.

| id | surface | class | typed arm | monolithic arm | justification | analysis treatment |
|---|---|---|---|---|---|---|
| mon1-01 | agent deliverable | intentionally-different | capability manifests + dora dataflow YAML + node modules of the agent region<br>`graphs/expert_t1.yaml` 303d08b37c6f<br>`registry/manifests/segmented-pose.yaml` db56206033d4<br>`registry/manifests/grasp-planner-topdown.yaml` fb4d4696b908<br>`registry/manifests/ik-trajectory.yaml` e2b498f5a47d<br>`registry/manifests/task-state-machine.yaml` 5af70b46a412<br>`src/aisle/nodes/segmented_pose.py` 78b748f4f0dc<br>`src/aisle/nodes/grasp_topdown.py` 3f24e0a56571<br>`src/aisle/nodes/ik_trajectory.py` 44e8186838d9<br>`src/aisle/nodes/task_state_machine.py` ad7fcc819c7b | one orchestration module against the primitive API<br>`experts/monolithic/expert_t1.py` 97ac76fcec0d<br>`docs/monolithic/allowlist.json` 975d05332e97 | this is the treatment: bundled typed-dataflow engineering surface versus one ordinary module | the manipulated factor (SPEC 500 primary contrast) |
| mon1-02 | static validation and diagnostics | intentionally-different | harness validate (VAL catalog: schema, topology, motion gating, turn plan)<br>`src/aisle/harness/validate.py` ed82967e800a | none; Python syntax/import/runtime errors only, via harness monolith check<br>`src/aisle/harness/monolith.py` 4beaa108af30 | part of the bundled treatment; absence is a declared property, not missing data (MON-12) | recorded as treatment property; check/run attempt and error events compared descriptively |
| mon1-03 | registry search and resolution | intentionally-different | registry manifests, harness registry search/lint<br>`src/aisle/harness/registry.py` 214da7bb8db2 | absent; the primitive API is the only capability catalog<br>`docs/monolithic/primitive-api.md` f6e5d9223e0f | part of the bundled treatment | treatment property |
| mon1-04 | documentation given to the agent | intentionally-different | research-agent contract and the typed graph/manifest docs<br>`harness/CLAUDE.research.md` e736bbd6caa7 | primitive API document and module contract<br>`docs/monolithic/primitive-api.md` f6e5d9223e0f<br>`docs/monolithic/README.md` 79d248b82b1e | each arm documents its own representation; task, goals, budgets and evaluator text are shared | documentation hashes in the MON-8 treatment tuple; representation-specific instructions listed here rather than in prose |
| mon1-05 | robot primitives (pose, grasp, trajectory, executor) | identical | called in-process by the typed nodes<br>`src/aisle/nodes/segmented_pose.py` 78b748f4f0dc<br>`src/aisle/nodes/grasp_topdown.py` 3f24e0a56571<br>`src/aisle/nodes/ik_trajectory.py` 44e8186838d9 | the same function objects re-exported by aisle.monolith.primitives<br>`src/aisle/monolith/primitives.py` d7d23dc527ca | none needed: same artifact | held fixed; pinned by tests/unit/test_monolith_surface.py (same objects) |
| mon1-06 | semantic observations | representation-equivalent | dora edges into the agent region (bridge_info, seg_overhead, depth_overhead, joint_state, episode_goal, episode_result, violation, reset_done, 1 Hz tick)<br>`graphs/expert_t1.yaml` 303d08b37c6f | the same fields as on_event(event) dicts from the broker<br>`graphs/monolithic_t1.yaml` a43b46fb6f7c<br>`docs/monolithic/interface-map.json` 4dffcb8a0bcb | none needed: same semantic fields, different envelope | held fixed; exactness checked by harness monolith interface |
| mon1-07 | actions and authority | representation-equivalent | joint_cmd / gripper_cmd edges into budget-guard, episode_feedback to rollout-client<br>`graphs/expert_t1.yaml` 303d08b37c6f | action dicts {joint_cmd}, {gripper_cmd}, {feedback} validated and published by the broker on the same edges<br>`src/aisle/nodes/monolith_broker.py` 2783a3282fb4 | none needed: same semantic fields, different envelope | held fixed; same names, arity and privilege (interface map) |
| mon1-08 | guard route and limits | identical | budget-guard node, harness/limits + budget.toml<br>`src/aisle/nodes/budget_guard.py` e15bc5f70b29<br>`harness/budget.toml` bcbb79aa146a | the same node consuming monolith-broker/joint_cmd and gripper_cmd<br>`graphs/monolithic_t1.yaml` a43b46fb6f7c | none needed: same artifact | held fixed (MON-5) |
| mon1-09 | trusted controller (bridge, reset, verifier, rollout client, turn barrier) | identical | as wired in expert_t1.yaml<br>`src/aisle/nodes/dora_genesis.py` c188e805ee48<br>`src/aisle/reset/service.py` 289a8c80374d<br>`src/aisle/verifier/oracle.py` c4ce22de0b88<br>`src/aisle/harness/rollout_client.py` 18808db0aa2d<br>`src/aisle/nodes/turn_barrier.py` fd075658ecc8 | the same nodes, same wiring, in monolithic_t1.yaml<br>`src/aisle/nodes/monolith_broker.py` 2783a3282fb4 | none needed: same artifact | held fixed; graphs differ only inside the agent region (tests/graph/test_monolithic_graph.py) |
| mon1-10 | transport metadata | intentionally-different | nodes see dora metadata (sim_time_ns, seq, env_id, turn stamps, h/w/enc)<br>`src/aisle/topics.py` 431a0e0bee4c | the module sees sim_time_ns only; frames arrive reshaped<br>`src/aisle/nodes/monolith_broker.py` 2783a3282fb4 | runtime envelope needed only by the typed transport; carries no task authority | excluded from task authority (MON-4); no analysis treatment |
| mon1-11 | turn accounting | intentionally-different | four lockstep participants in the agent region<br>`graphs/turn_plans/expert_t1.json` 6f0c8997c748 | one participant (the broker)<br>`graphs/turn_plans/monolithic_t1.json` d268bcb32285 | runtime representation of the same synchronous step; ordering of the four typed nodes is fixed by the plan, so the merged participant observes the same data per turn | no task-outcome treatment; wall-time is reported per arm, not compared causally |
| mon1-12 | in-episode retry budget | intentionally-different | AISLE_MAX_RETRIES=8 attested in the graph<br>`graphs/expert_t1.yaml` 303d08b37c6f | module-internal decision, bounded by the same episode timeout<br>`experts/monolithic/expert_t1.py` 97ac76fcec0d | the typed arm attests retries in YAML because that is where its decisions live; the episode budget (timeout, seeds) is held by the rollout runner in both arms | retries reported per episode in both arms (episode_feedback.retries) |
| mon1-13 | episode budget, seeds, reset semantics, evaluator | identical | harness rollout --graph graphs/expert_t1.yaml<br>`src/aisle/harness/rollout.py` 302919a9c2fb | harness monolith run --module (calls the same rollout())<br>`src/aisle/harness/monolith.py` 4beaa108af30 | none needed: same artifact | held fixed |
| mon1-14 | confinement | intentionally-different | issue #353 OS-level policy (pending)<br>`specs/460-actuation-threat-model.md` 27c3701afb0b | broker import/builtins hook + integrity check now; #353 policy pending<br>`src/aisle/monolith/confinement.py` 3a4114da57bc | the module runs in the broker's process, so an in-process denial layer is needed in addition to #353 | infrastructure-invalid records excluded in both arms; confinement status in the MON-8 tuple |
