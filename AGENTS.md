# AGENTS.md — Codex entry point

Codex: read CLAUDE.md — it is the single agent contract for this repo and is
tool-agnostic despite the filename. All gates, rules, and the spec-driven loop
apply identically. Cross-review protocol (CON-16): when Claude Code authored a
PR, you review it (and vice versa); findings go in PR comments, never direct pushes.
